using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BallCollisionDetection : MonoBehaviour
{
    /*
     To detect which side of the pitch the ball is on, and to detect which face of the net the ball hits
     to determine which player can hit ball and who scores respective
         */

    // Start is called before the first frame update
    void Start()
    {
        
    }
    public GameObject p1;
    public GameObject p2;


    // Update is called once per frame
    void Update()
    {
        
    }
}
